# Runtime image — Go binary is cross-compiled on the laptop (no compile here).
# Build context: deploy/
FROM alpine:3.20
RUN apk add --no-cache ca-certificates
COPY artifacts/api /usr/local/bin/api
RUN chmod +x /usr/local/bin/api
EXPOSE 8080
ENTRYPOINT ["/usr/local/bin/api"]
