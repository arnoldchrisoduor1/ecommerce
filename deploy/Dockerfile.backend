# One-time base image: alpine + CA certs.
# The Go binary is bind-mounted at runtime (see docker-compose.prod.yml) —
# do NOT COPY artifacts here (that made deploys thrash a 400MB VPS).
FROM alpine:3.20
RUN apk add --no-cache ca-certificates
ENTRYPOINT ["/usr/local/bin/api"]
