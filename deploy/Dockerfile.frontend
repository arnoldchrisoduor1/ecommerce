# Runtime image — Next.js standalone tree is prebuilt on the laptop.
# Build context: deploy/
FROM node:22-alpine
WORKDIR /app
ENV NODE_ENV=production
ENV PORT=3000
ENV HOSTNAME=0.0.0.0
COPY artifacts/frontend/ ./
EXPOSE 3000
USER node
CMD ["node", "server.js"]
