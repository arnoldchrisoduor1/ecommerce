# Unused when compose uses image: node:22-alpine + bind mount.
# Kept so `docker compose build frontend` still works if someone runs it.
FROM node:22-alpine
WORKDIR /app
ENV NODE_ENV=production
ENV PORT=3000
ENV HOSTNAME=0.0.0.0
USER node
CMD ["node", "server.js"]
